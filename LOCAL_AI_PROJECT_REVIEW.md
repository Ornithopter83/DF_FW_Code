# DF Firmware·TestModule 프로젝트: 로컬 AI에 필요한 추론 수준

작성일: 2026-09-02

## 결론

이 프로젝트는 **기능 수보다 장비 상태와 통신 순서를 함께 다뤄야 한다는 점에서 추론 부담이 큰 프로젝트**다. 한 함수나 화면을 수정하는 작업은 보통 수준의 추론으로 처리할 수 있지만, MAIN·ROD·PC 프로그램에 걸친 업데이트와 재연결 문제는 높은 수준의 추론이 필요하다.

로컬 AI 도입을 고민한다면 “프로젝트 전체를 한 번에 만들 수 있는가”보다 **작업 범위를 지키고, 관련 코드를 정확히 읽고, 여러 장치의 상태 변화를 추적할 수 있는가**를 기준으로 평가하는 편이 적절하다.

## 이 리뷰의 근거와 한계

이 문서는 이미 읽은 `AGENTS.md`, `NewThreadHandoff.md` 및 이번 대화에 근거한다. 이번 작성에서는 소스 탐색, 코드 줄 수 집계, 빌드, 테스트, 모델 비교 실험을 수행하지 않았다.

따라서 아래 추론 수준은 작업 특성에 대한 정성적 평가다. 과거 구현에 실제로 사용한 모델의 추론 설정이나 토큰 소비량을 측정한 결과는 아니다. 특정 로컬 모델이나 파라미터 수에 대한 성능 보장도 아니다.

## 1. 프로젝트 규모를 어떻게 봐야 하는가

확인된 구성은 세 개의 주요 실행 주체와 공유 프로토콜이다.

| 구성 | 역할 | AI가 함께 고려할 내용 |
|---|---|---|
| MAIN 펌웨어 | PC와 통신하고 ROD 무선 업데이트를 중계 | polling, 명령 처리, 업데이트 중 상태 변화 |
| ROD 펌웨어 | 장비 기능과 무선 업데이트 대상 | 전송 완료, 재연결, 버전 확인 |
| PC TestModule(TM) | 연결·상태 조회·업데이트·LM JIG 제어 | 사용자 화면과 실제 장비 상태의 일치 |
| 공유 프로토콜 | 각 구성 사이의 명령과 데이터 규칙 | 메시지 형식, ACK, 시간 제한, 호환성 |

여기에 단일 EXE 배포, 내장 esptool을 사용하는 유선 업데이트, 기존 원본과 제거 코드 보존, C++98/03 제약이 더해진다. C++98/03 등의 펌웨어 규칙을 TM의 언어·프레임워크 요구사항으로 임의 확장해서는 안 된다.

코드량 기준으로 소형·중형·대형을 확정할 자료는 없다. 다만 **여러 실행 주체와 하드웨어를 연동하는 프로젝트**이므로, 단일 화면 프로그램보다 변경 영향과 오류 원인을 추적하기 어렵다.

인수인계상 최근 구현 범위는 11-J 무선 업데이트 보완과 11-K LM JIG 제어였으며, 두 항목 모두 구현 완료·활성 구현 잔여 0개로 기록되어 있다. 이것을 프로젝트 전체 또는 물리 장비 검증의 완료로 해석하면 안 된다.

## 2. 어느 작업에 어느 정도의 추론이 필요한가

여기서 낮음·보통·높음은 비교를 위한 표현이다. 로컬 추론 서버의 특정 설정값과 일대일로 대응하지 않는다.

| 작업 | 필요한 추론 수준 | 판단 이유 |
|---|---|---|
| 정해진 양식으로 상태 문서 정리 | 낮음~보통 | 사실을 추려 쓰는 작업이지만, 미검증을 완료로 바꾸지 않아야 함 |
| 단일 화면 문구·표시 수정 | 보통 | 수정 범위가 작고 입출력이 비교적 명확함 |
| 기존 형식에 맞춘 명령 하나 추가 | 보통 | 송신뿐 아니라 응답 처리와 기존 규칙을 확인해야 함 |
| C++98/03 제약을 지키는 구조 정리 | 보통~높음 | 선언·정의·호출 관계와 기존 동작을 함께 보존해야 함 |
| 무선 업데이트 후 polling·재연결·버전 조회 | 높음 | 여러 장치의 비동기 상태와 완료 조건이 서로 연결됨 |
| LM JIG 명령과 모터 정지 조건 연결 | 높음 | PC 명령, 펌웨어 제어, 센서, 시간 제한을 구분해야 함 |
| 장비·통신·PC 환경에 걸친 장애 분석 | 높음 | 관찰 결과로 여러 원인 후보를 구분해야 함 |

전체 작업을 항상 가장 높은 추론 수준으로 실행할 필요는 없다. **수정할 코드가 명확하고 영향 범위가 좁으면 보통 수준**, 여러 상태와 예외 경로를 동시에 검토해야 하면 높은 수준이 적합하다.

## 3. 이 프로젝트에서 추론이 특히 필요한 이유

### 전송 완료와 장비 사용 가능 상태가 다르다

ROD 무선 업데이트에서는 데이터 전송이 끝났다는 사실만으로 전체 성공을 선언할 수 없다. MAIN의 polling 재개, ROD 재연결 대기, 버전 조회, 완료 화면에 표시할 결과를 함께 판단해야 한다.

AI가 각 함수를 개별적으로 잘 작성해도 이 순서를 놓치면 화면상 성공과 실제 장비 상태가 어긋날 수 있다. 이 부분은 문법 지식보다 상태 전이를 추적하는 능력이 중요하다.

### 비슷해 보이는 메시지의 의미를 구분해야 한다

인수인계에는 `$1800%`가 MAIN에서 AP로 보내는 종료 요청이며, 자동 완료 ACK는 제거했다고 기록되어 있다. 메시지가 보인다는 이유만으로 오류로 간주하거나 관습적인 ACK를 추가하면 기존 의도를 훼손할 수 있다.

따라서 코드 패턴만 보고 채우기보다 프로토콜과 사용자 관찰을 근거로 판단해야 한다. 명령의 정확한 의미가 없으면 추측 대신 필요한 자료를 요청하는 것이 올바른 동작이다.

### 코드로 확인할 수 없는 영역이 있다

LM JIG는 센서 도달 또는 3초 제한에서 정지하도록 구현되었다고 기록되어 있지만 물리 장비 동작은 미검증이다. 높은 추론 능력도 실제 배선, 센서 반응, 모터 동작을 대신 확인할 수 없다.

코드 검토·빌드 성공·실제 장비 성공을 구분하는 능력은 이 프로젝트에서 중요한 평가 항목이다.

## 4. 로컬 AI를 고를 때 볼 능력

모델 이름이나 추론 출력의 길이만으로 판단하기보다 다음 항목을 실제 작업에서 확인해야 한다.

1. **지시 준수:** 승인받지 않은 빌드·탐색·재시도를 멈추는가? 제외된 작업을 다시 시작하지 않는가?
2. **코드 이해:** 정의·호출부·응답 처리까지 찾아 연결하고, 실제 파일을 근거로 설명하는가?
3. **상태 추적:** 정상 흐름뿐 아니라 응답 지연, 연결 끊김, 재연결 실패를 구분하는가?
4. **제약 준수:** 펌웨어의 C++98/03, namespace 금지, 기존 코드 보존 규칙을 일관되게 지키는가?
5. **범위 유지:** 작은 수정 요청에 불필요한 재설계나 대규모 변경을 덧붙이지 않는가?
6. **불확실성 표현:** 모르는 명령 형식을 만들거나, 수행하지 않은 검증을 성공으로 보고하지 않는가?

문맥 창이 큰 것과 긴 문서에서 중요한 제약을 기억하는 것은 다르다. 필요한 자료를 적절히 나누어 제공하되, 여러 파일을 읽은 뒤에도 최초 지침을 유지하는지 봐야 한다.

또한 모델과 실행 도구는 별개다. 이번 대화에서는 샌드박스의 Arduino 폴더 ACL 설정 실패와 PowerShell 스크립트 실행 정책이 각각 작업을 막았다. 이런 실행 환경 문제를 모델의 코드 작성 능력 부족으로 오해하면 평가가 왜곡된다. 반대로 모델이 원인을 구분하지 못하고 무작정 재시도하는 것도 좋은 수행은 아니다.

## 5. 비교 평가를 한다면

다음은 향후 사용자 승인 후 수행할 수 있는 평가 제안이며, 이번에 실행한 테스트가 아니다.

| 단계 | 동일하게 제공할 과제 | 주로 볼 항목 |
|---|---|---|
| 1 | 두 인수인계 문서를 읽고 짧게 상태 보고 | 추가 탐색 금지, 미검증 구분, 간결성 |
| 2 | 관련 파일을 주고 명령 하나의 송수신 경로 설명 | 근거 파일, 호출 관계, 추측 여부 |
| 3 | 범위를 지정한 작은 수정 하나 | 정확성, 기존 변경 보존, 불필요한 수정 여부 |
| 4 | OTA 전송 후 재연결 실패 사례 분석 | 상태 구분, 원인 후보, 추가로 필요한 증거 |

모델 간 비교에서는 지침·자료·도구 권한·과제를 같게 맞춘다. 최초 정답 여부와 함께 사람이 수정한 횟수, 승인 위반, 허위 완료 보고, 처리 시간을 기록하면 실용성을 판단하기 쉽다.

단순 과제를 잘 수행했다고 OTA 전체 구현 능력이 입증되는 것은 아니다. 반대로 복잡한 과제에서 도움이 필요하더라도 문서 정리나 좁은 수정 작업에는 충분히 활용할 수 있다.

## 6. 도입 판단

이 프로젝트에서 로컬 AI의 첫 적용 범위로는 **인수인계 요약, 제한된 코드 설명, 명확히 지정된 작은 수정**이 적절하다는 판단이다. 이 과제에서 지시 준수와 정확성이 확인되면 통신 흐름 분석과 여러 파일의 변경으로 범위를 넓힐 수 있다.

MAIN·ROD·TM을 함께 변경하는 작업은 높은 수준의 추론과 별도 검토가 필요하다. 모델이 생성한 코드가 그럴듯하다는 이유만으로 승인하지 말고, 상태별 동작과 검증 근거를 설명할 수 있는지 확인해야 한다.

현재 자료만으로 적정 파라미터 수, 양자화 수준, VRAM 용량을 단정할 수는 없다. **필요한 작업의 난이도와 허용 가능한 사람의 개입량을 먼저 정하고, 위 과제의 실제 결과로 모델과 실행 설정을 선택하는 것이 이 프로젝트에 맞는 평가 방법이다.**
