# DF Main/Rod 통합 펌웨어 리팩토링 로드맵

Updated: 2026-08-27

## 목표

`Vm1.0.9.0` Main 펌웨어와 `Vr1.0.1.0` Rod 펌웨어를 하나의 저장소, 빌드 기준, 프로토콜 계약, 작업 관리 및 릴리스 체계에서 관리한다. 두 펌웨어는 독립 바이너리로 유지하되 현재 사용하는 하드웨어와 기능만 남기고, 대규모 `.ino`를 C++98/03 호환 `.cpp/.h` 모듈로 단계적으로 분리한다.

```text
DF Firmware Repository
├─ Main Firmware ─┐
│                 ├─ DF Protocol Contract ── Manual Main/Rod Integration Test
└─ Rod Firmware ──┘
```

## 작업서

| No. | 작업서 | 목적 | 상태 |
| --- | --- | --- | --- |
| 01 | [현재 제품 기준선 확정](tasks/01_현재제품_기준선_확정.md) | 원본, 현재 하드웨어, Arduino 참조, 수동 동작 기준 확정 | 진행 |
| 02 | [재현 빌드와 VS2022 환경](tasks/02_재현빌드_VS2022_환경.md) | Main/Rod 명령행 빌드와 VS2022 연결 | 대기 |
| 03 | [최신 구성 단일화와 구 코드 격리](tasks/03_최신구성_단일화_구코드_격리.md) | Variant 분기 제거 및 deprecated 보존 | 대기 |
| 04 | [공유 프로토콜 계약](tasks/04_공유프로토콜_계약.md) | Main/Rod 메시지 정의 단일화와 호환 검사 준비 | 대기 |
| 05 | [Main 물리적 모듈 분리](tasks/05_Main_물리적_모듈분리.md) | `DF_Main.ino` 기능별 분리 | 대기 |
| 06 | [Rod 물리적 모듈 분리](tasks/06_Rod_물리적_모듈분리.md) | `DF_Rod.ino` 기능별 분리 | 대기 |
| 07 | [상태와 Callback 안전성 개선](tasks/07_상태_Callback_안전성.md) | 전역 상태, 초기화, 수신 경계 안전화 | 대기 |
| 08 | [통합 검증과 릴리스](tasks/08_통합검증_릴리스.md) | 현재 Main/Rod 한 쌍의 수동 검증 및 릴리스 자료 확정 | 대기 |

## 진행 순서

01 → 02 → 03 → 04 → 05 → 06 → 07 → 08

작업 05와 06은 작업 04 완료 후 순서를 바꿀 수 있으나 동시에 대규모 변경하지 않는다.

## 운영 규칙

- 한 번에 한 번호 작업서만 활성화한다.
- 작업서 안에서도 한 번에 한 문자 단위만 처리한다.
- 코드 변경과 동작 변경을 같은 문자 작업에 섞지 않는다.
- 수동 빌드 또는 장비 검증 게이트에 도달하면 Codex는 중단하고 사용자에게 필요한 실행 내용과 결과 항목을 알린다.
- 사용자 결과를 받기 전에는 빌드, 플래시, smoke/HIL 검증을 완료로 기록하지 않는다.
- 제거 코드는 먼저 `deprecated/`에 출처와 제거 이유를 기록한 뒤 활성 코드에서 제거한다.

## 공통 변경 금지 경계

- 현재 Main/Rod 메시지 PID, payload 형식, 타이밍 및 장비 동작을 의도 없이 변경하지 않는다.
- `Vm1.0.9.0`과 `Vr1.0.1.0` 기준 원본을 덮어쓰지 않는다.
- C++11 이후 문법, 람다, 스마트 포인터 및 namespace를 도입하지 않는다.
- 과거 Variant를 활성 빌드 경로에 다시 도입하지 않는다.
- ESP32 Arduino Core는 기준선 확인 전 2.0.17에서 임의 업그레이드하지 않는다.
- 사용자의 명시적 요청 없이 commit, push, flash 또는 장비 제어를 하지 않는다.

## 목표 구조

```text
CodexManage/
├─ legacy/                 # 전체 기준 원본
├─ deprecated/             # 활성 코드에서 제거한 구 코드, 빌드 제외
├─ firmware/
│  ├─ DF_Main/
│  └─ DF_Rod/
├─ libraries/DFProtocol/   # C++03 호환 공유 메시지 계약
├─ docs/
├─ tools/
├─ tests/
├─ tasks/
└─ artifacts/              # 생성물, Git 제외
```

## 현재 상태

현재 작업: 01 현재 제품 기준선 확정

잔여 작업 8개 (01, 02, 03, 04, 05, 06, 07, 08)

